[Unit]
Description=Restore the keyboard backlight that locking the session turns off
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/kbd-backlight-restore
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
