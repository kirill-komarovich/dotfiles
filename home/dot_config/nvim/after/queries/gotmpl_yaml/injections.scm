; The host language of a chezmoi template, named per flavour so mixed buffers
; keep their own layer instead of racing over one global query.
((text) @injection.content
  (#set! injection.language "yaml")
  (#set! injection.combined))
